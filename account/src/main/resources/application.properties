# Raft：为 true 时资金锁（LockRequest）经共识提交后再写 AccountCache 并回 Aeron；成交流仍直接走 AccountCache（后续可再接 Raft）
account.raft.enabled=false
account.raft.data-path=${java.io.tmpdir}/peanuts-account-raft
account.raft.group-id=peanuts-account
account.raft.server-id=127.0.0.1:8811
account.raft.init-conf=127.0.0.1:8811
